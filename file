#include<stdio.h>
void main()
{
  int n, r = 0, t;

  printf("Enter a number\n");
  scanf("%d", &n);

 for(t=n; t!=0; t /= 10)
  {
    r *= 10;
    r += t%10;
  }

  if (n == r)
    printf("%d is a palindrome number.\n", n);

  else
    printf("%d is not a palindrome number.\n", n);
}
